export const Buttons = ({ onClick }) => {
  return (
    <div>
      <button
        className="bg-black w-104 h-11 rounded-lg cursor-pointer"
        onClick={onClick}
      >
        Continue 1/3
      </button>
    </div>
  );
};
